{
  "name": "confeitaria-backend",
  "version": "1.0.0",
  "description": "Backend for Confeitaria Admin Module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "tap --ts src/**/*.test.ts"
  },
  "dependencies": {
    "@fastify/cookie": "^9.0.0",
    "@fastify/cors": "^8.3.0",
    "@fastify/jwt": "^7.2.0",
    "@prisma/client": "^5.0.0",
    "bcrypt": "^5.1.0",
    "fastify": "^4.20.0",
    "zod": "^3.21.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/node": "^20.0.0",
    "prisma": "^5.0.0",
    "tap": "^16.3.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "prisma": {
    "seed": "ts-node-dev prisma/seed.ts"
  }
}